#include<stdio.h>
int main()
{
    float num1,num2,sum;
    printf("Enter two number");
    scanf("%lf",&num1);
        scanf("%lf",&num2);
        sum=num1+num2;
        printf("Sum is %.2lf",sum);
        return 0;

}
